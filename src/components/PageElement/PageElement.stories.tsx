/* eslint-disable */
import React from 'react';
import { storiesOf } from '@storybook/react';
import PageElement from './PageElement';

storiesOf('PageElement', module).add('default', () => <PageElement />);
